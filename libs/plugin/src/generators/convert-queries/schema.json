{
	"$schema": "http://json-schema.org/schema",
	"$id": "",
	"title": "",
	"type": "object",
	"properties": {
		"project": {
			"type": "string",
			"aliases": ["name", "projectName"],
			"description": "Project for which to convert to new content queries."
		},
		"path": {
			"type": "string",
			"description": "Path to the component/directive you want to convert to use new content queries."
		}
	}
}
